server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://3.26.101.146:3306/petcare?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        jdbc:
          time_zone: Asia/Seoul
    show-sql: true

jwt:
  secret: ${JWT_SECRET:petCareSecretKeyForJWTTokenGenerationMustBeLongEnoughForHS256Algorithm}
  expiration: ${JWT_EXPIRATION:86400000}